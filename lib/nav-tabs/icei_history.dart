import 'package:flutter/material.dart';

class ICEIHistory extends StatelessWidget {
  const ICEIHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("ICEI History"),
        ),
      ),
    );
  }
}
